import React from 'react';
import styled from 'styled-components';

interface Props {}

const Header: React.FC<Props> = () => <Wrapper>header</Wrapper>;

export default Header;

const Wrapper = styled.header``;
