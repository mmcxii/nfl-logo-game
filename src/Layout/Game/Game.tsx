import React from 'react';
import styled from 'styled-components';

interface Props {}

const Game: React.FC<Props> = () => <Wrapper>Game</Wrapper>;

export default Game;

const Wrapper = styled.main``;
